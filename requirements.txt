# 런타임 의존성 — 버전 고정.
# 고정하는 이유: 재현성 요구(docs/problem-statement.md §2) 때문이다. 동일 질의에
# 같은 결과를 내려면 파이프라인 구성이 같아야 하고, 여기엔 라이브러리 버전도 포함된다.
# 올릴 때만 의도적으로 올린다.
#
# 검증 환경: Python 3.14.0 / Windows. session-02에서 실제 엔드포인트 호출 확인.

# 오케스트레이션 (ADR-002)
langgraph==1.1.4

# 모델 프로바이더 추상화 — OpenAI 호환 엔드포인트 호출 (ADR-003)
litellm==1.101.0

# .env 로딩 (엔드포인트·모델을 코드에 고정하지 않기 위함)
python-dotenv==1.1.1
